# Padestrian-Detection
This project implements a Pedestrian Detection System using Python and OpenCV. The system detects human pedestrians in video footage by applying the Histogram of Oriented Gradients (HOG) feature descriptor combined with a pre-trained Support Vector Machine (SVM) classifier.
